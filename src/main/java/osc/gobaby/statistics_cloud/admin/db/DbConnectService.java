package osc.gobaby.statistics_cloud.admin.db;

import org.springframework.stereotype.Service;
import osc.gobaby.statistics_cloud.admin.db.entity.DbConnect;

@Service
public class DbConnectService {

    public boolean init(DbConnect dbConnect){

        //@TODO
        //1. db정보 파일로 저장
        //2. ddl - create db / table / insert admin user

        return true;
    }
}
